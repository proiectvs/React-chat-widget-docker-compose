# React-chat-widget-docker-compose
React-chat is an client/server char written in react.js . 
For backend I used Flask (python) 
Both the client and the server are cointainerized using docker-compose.
